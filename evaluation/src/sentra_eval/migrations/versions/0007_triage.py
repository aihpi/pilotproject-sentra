"""Sample seed and rate on the run

Stufe 3 draws a sample of the cases Stufe 1 found unremarkable, to detect Stufe
1 systematically missing things. That only supports the claim if the sample can
be reconstructed, so the seed is part of the round's record rather than
something regenerated on read — "why was this case not reviewed" then has an
answer better than a shrug.

The rate is stored for the same reason: changing it later must not change what
a finished round did.


Revision ID: 94cebb07114d
Revises: 0006_verdicts
Create Date: 2026-09-17 19:42:05.176300
"""

from collections.abc import Sequence

import sqlalchemy as sa
from alembic import op

revision: str = "0007_triage"
down_revision: str | None = "0006_verdicts"
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None


def upgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    op.add_column("runs", sa.Column("stichprobe_seed", sa.Integer(), nullable=False))
    op.add_column("runs", sa.Column("stichprobe_anteil", sa.Float(), nullable=False))
    # ### end Alembic commands ###


def downgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_column("runs", "stichprobe_anteil")
    op.drop_column("runs", "stichprobe_seed")
    # ### end Alembic commands ###
