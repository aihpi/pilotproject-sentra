"""ragas opt-in on the run

Off by default: three LLM-scored metrics per answer roughly doubles a round's
model calls. Stored rather than read from configuration, because a round scored
with ragas and a round scored without it are not the same evidence, and a
setting that changed afterwards would silently rewrite what a finished round
means.


Revision ID: 2a912d562503
Revises: 0009_feedback_origin
Create Date: 2026-09-17 21:08:44.741313
"""

from collections.abc import Sequence

import sqlalchemy as sa
from alembic import op

revision: str = "0010_ragas"
down_revision: str | None = "0009_feedback_origin"
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None


def upgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    op.add_column("runs", sa.Column("ragas_aktiv", sa.Boolean(), nullable=False))
    # ### end Alembic commands ###


def downgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_column("runs", "ragas_aktiv")
    # ### end Alembic commands ###
