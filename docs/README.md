# docs/

Reserved for generated documentation — Sphinx or MkDocs — and empty until one
of them is set up.

Hand-written design notes and documents we received live in `references/`, not
here. The split is deliberate: this directory is expected to hold build output
and its source tree, so a note left in it would be indistinguishable from
something a generator produced.
