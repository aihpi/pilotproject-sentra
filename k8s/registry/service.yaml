apiVersion: v1
kind: Service
metadata:
  name: sentra-db
  labels:
    app: sentra-registry
spec:
  type: ClusterIP
  selector:
    app: sentra-registry
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
