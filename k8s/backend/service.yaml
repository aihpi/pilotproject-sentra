apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: sentra-backend
spec:
  type: ClusterIP
  selector:
    app: sentra-backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
