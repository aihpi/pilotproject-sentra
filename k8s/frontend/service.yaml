apiVersion: v1
kind: Service
metadata:
  name: sentra-frontend
  labels:
    app: sentra-frontend
spec:
  type: LoadBalancer
  selector:
    app: sentra-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
